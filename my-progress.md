# my-progress

> Phase 0 / Lesson 02 (Git & Collaboration) hands-on notes.
> Branch: `my-progress` (created from `main`).

## What I did

1. Cloned `https://github.com/rohitg00/ai-engineering-from-scratch.git`.
2. Created a personal branch `my-progress` for my work.
3. Confirmed the repo's `.gitignore` already excludes model checkpoints
   (`.pt`, `.pth`, `.safetensors`) and added a small personal section
   for scratch files.
4. Wrote this file as the first commit on the branch.
5. Plan to push to my own GitHub fork once auth is set up.

## Network note (China)

Direct HTTPS to GitHub was reset by the network. Worked around it by
routing `git` through a local proxy at `127.0.0.1:7890` (Dove) via
`git config --global http(s).proxy`. Will revisit when pushing.
